package request

type CreateSellerRequest struct {
	TODO string `json:"TODO" example:"Puede con el mismo dispositivo planificar y llevarse la ruta (actua como un despachador y conductor a la vez)."`
}
